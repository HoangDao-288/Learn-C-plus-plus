1. this pointer

- Là một con trỏ ẩn được compiler tự động truyền vào mọi non-static member function, 
trỏ tới object đang gọi hàm đó.

    1.1 static member function

    1.2 const member function

    1.3 Phân biệt this / *this

2. virtual / virtual =0 function

3. Overloading / Overriding

4. Phân biệt 4 tính chất của OOP được thẻ hiện trong class